import React, { useState } from 'react'

function Trailer({ modalOpened, setModalOpened }) {
    const [showModal, setShowModal] = useState(false);
    return (
        <>
           
        </>
    )
}

export default Trailer